{
  "name": "web3authn-passkey-monorepo",
  "private": true,
  "scripts": {
    "install-all": "pnpm -w install",
    "build:sdk": "pnpm -C packages/passkey build",
    "dev": "pnpm -C frontend dev",
    "server": "pnpm -C relay-server dev"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0"
  }
}
