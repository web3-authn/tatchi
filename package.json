{
  "name": "web3authn-passkey-monorepo",
  "private": true,
  "scripts": {
    "install-all": "pnpm -w install",
    "build:sdk": "pnpm -C passkey-sdk build",
    "frontend": "pnpm -C examples/vite dev",
    "server": "pnpm -C examples/relay-server dev",
    "gen:shamir": "pnpm -C examples/relay-server run gen:shamir"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0"
  }
}
