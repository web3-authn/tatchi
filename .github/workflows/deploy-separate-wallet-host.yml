name: deploy-separate-wallet-host

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

concurrency:
  group: separate-wallet-host-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies (workspace)
        run: pnpm install --no-frozen-lockfile

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Add wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - name: Install wasm-pack
        run: |
          curl -sSf https://rustwasm.github.io/wasm-pack/installer/init.sh | sh

      - name: Generate WASM TypeScript types (wasm-pack)
        run: pnpm -C passkey-sdk exec bash ./scripts/generate-types.sh

      - name: Build SDK (@web3authn/passkey)
        run: pnpm --filter @web3authn/passkey build

      - name: Build wallet host example
        env:
          VITE_WALLET_ORIGIN: https://web3authn.org
          VITE_WALLET_SERVICE_PATH: /wallet-service
          VITE_SDK_BASE_PATH: /sdk
          VITE_RP_ID_BASE: web3authn.org
          VITE_RELAYER_URL: ${{ secrets.VITE_RELAYER_URL }}
          VITE_RELAYER_ACCOUNT_ID: ${{ secrets.VITE_RELAYER_ACCOUNT_ID }}
          VITE_NEAR_NETWORK: ${{ secrets.VITE_NEAR_NETWORK || 'testnet' }}
          VITE_NEAR_RPC_URL: ${{ secrets.VITE_NEAR_RPC_URL || 'https://test.rpc.fastnear.com' }}
          VITE_NEAR_EXPLORER: ${{ secrets.VITE_NEAR_EXPLORER || 'https://testnet.nearblocks.io' }}
        run: pnpm -C examples/wallet-scoped-credentials build

      - name: Copy SDK assets into Pages output
        run: |
          mkdir -p examples/wallet-scoped-credentials/dist/sdk
          rsync -av --delete passkey-sdk/dist/esm/sdk/ examples/wallet-scoped-credentials/dist/sdk/

      - name: Emit static ROR manifest (optional)
        if: ${{ secrets.ROR_ALLOWED_ORIGINS != '' }}
        env:
          ROR_ALLOWED_ORIGINS: ${{ secrets.ROR_ALLOWED_ORIGINS }}
        run: |
          mkdir -p examples/wallet-scoped-credentials/dist/.well-known
          printf '{\n  "origins": [\n' > examples/wallet-scoped-credentials/dist/.well-known/webauthn
          IFS=',' read -ra ORIGS <<< "$ROR_ALLOWED_ORIGINS"
          for i in "${!ORIGS[@]}"; do
            ORIGIN=$(echo "${ORIGS[$i]}" | xargs)
            if [ -n "$ORIGIN" ]; then
              if [ $i -gt 0 ]; then printf ',\n' >> examples/wallet-scoped-credentials/dist/.well-known/webauthn; fi
              printf '    "%s"' "$ORIGIN" >> examples/wallet-scoped-credentials/dist/.well-known/webauthn
            fi
          done
          printf '\n  ]\n}\n' >> examples/wallet-scoped-credentials/dist/.well-known/webauthn

      - name: Deploy to Cloudflare Pages (wallet host)
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ secrets.CF_PAGES_PROJECT_WALLET }}
          directory: examples/wallet-scoped-credentials/dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

