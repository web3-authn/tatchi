import { TatchiPasskeyProvider } from '@tatchi/sdk/react'

// SDK config (wallet origin, optional relayer, etc.)
const config = { walletOrigin, ...configs }

export function App() {
  return (
    <TatchiPasskeyProvider config={config}>
      {/* your app */}
    </TatchiPasskeyProvider>
  )
}

