# Relayer configuration
VITE_RELAYER_URL=https://relay-server.localhost
VITE_RELAYER_ACCOUNT_ID=w3a-relayer.testnet

# Wallet iframe configuration
VITE_WALLET_ORIGIN=https://wallet.example.localhost
VITE_RP_ID_BASE=example.localhost
VITE_WEBAUTHN_CONTRACT_ID=w3a-v1.testnet

# Cross-origin isolation (COEP/CORP)
# Default: COEP off on app pages (extension embedding + password managers).
# Set to `strict` only when you need cross-origin isolation.
VITE_COEP_MODE=off

# Phase 0 (extension embedding) helpers
# - Point the wallet origin at the extension to test Architecture A embedding:
#   VITE_WALLET_ORIGIN=chrome-extension://<extension_id>
# - If you need cross-origin isolation again:
#   VITE_COEP_MODE=strict

# Optional overrides
# VITE_NEAR_NETWORK=testnet
# VITE_NEAR_RPC_URL=https://test.rpc.fastnear.com
